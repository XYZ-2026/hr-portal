# Nginx config for Hostinger VPS
# Place at: /etc/nginx/sites-available/hr-portal

server {
    listen 80;
    server_name hr.collegesimplified.in www.hr.collegesimplified.in;

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$host$request_uri;

    # Next.js frontend
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # FastAPI backend — proxied at /api
    location /api/ {
        rewrite ^/api/(.*) /$1 break;
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Increase timeout for PDF generation
        proxy_read_timeout 120s;
        proxy_connect_timeout 120s;
    }
}

# HTTPS config (uncomment after certbot SSL setup)
# server {
#     listen 443 ssl http2;
#     server_name hr.collegesimplified.in www.hr.collegesimplified.in;
#
#     ssl_certificate /etc/letsencrypt/live/hr.collegesimplified.in/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/hr.collegesimplified.in/privkey.pem;
#
#     # Same location blocks as above...
# }
